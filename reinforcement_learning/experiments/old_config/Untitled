# -------------------------------------------------------------------
# 状態空間エンコーディング設定（コンパクトモードを無効化）
# config.yamlのcompactモード設定を上書きしてfullモードに戻す
# -------------------------------------------------------------------
state_encoding:
  mode: 'full'  # 通常モード（999次元）を使用
  # top_k設定はfullモードでは無効
  # normalization設定はfullモードでも使用可能（必要に応じて）